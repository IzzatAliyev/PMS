<a asp-area="" asp-controller="Project" asp-action="GetTasksById">
    <div class="tile">
        <h2>Project 1</h2>
        <p class="description">Description of project 1.</p>
        <div class="status success">Complete</div>
        <p class="task">To tasks</p>
    </div>
</a>

<style>
    p.task{
        text-align: right;
        font-size: 20px;
        margin-bottom: auto;
        color: var(--default-color);
    }

    .tile {
        background-color: var(--third-color);
        margin-bottom: 10px;
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        transition: box-shadow 0.3s ease-in-out;
    }

    .tile:hover {
        box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.2);
    }

    .tile h2 {
        margin-top: 0;
        font-size: 24px;
        font-weight: bold;
        color: var(--text-color);
    }

    p.description {
        margin-bottom: 0;
        font-size: 16px;
        color: var(--default-color);
    }

    .tile .status {
        margin-top: 10px;
        display: inline-block;
        padding: 5px 10px;
        border-radius: 20px;
        font-size: 14px;
        font-weight: bold;
        color: white;
        text-transform: uppercase;
    }

    .tile .status.success {
        background-color: #4caf50;
    }

    .tile .status.warning {
        background-color: #ff9800;
    }

    .tile .status.error {
        background-color: #f44336;
    }
</style>